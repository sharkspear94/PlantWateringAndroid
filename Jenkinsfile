pipeline{
    agent any

    stages("build") {
        steps {
            echo 'building the app'
        }
    }

    stages("test") {
            steps {
                echo 'Testing the app'
            }
        }

    stages("deploy") {
            steps {
                echo 'deploying the app'
            }
        }

}